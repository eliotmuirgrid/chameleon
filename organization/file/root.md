# Organization / File / Root

Always name action files with the base verb (root form), not a changed form.

Use `raise`, not `raises`, `raising`, or `raised`.

Rename all files not using this convention to the root form.