# Tracker — command-line executable.
#
# Sources under ../BAS and ../TSV are linked into the binary (shared makefile
# pattern). Local sources live in this directory.

DIRS := BAS TSV

TARGET := tracker

include ../make/makefile.core
