# Git `core.ignorecase` and filesystems (detail pages)

Optional depth for [Git `core.ignorecase` and filesystems](../git/core/ignorecase.md). The hub page states the **overload signal**, what Git must reconcile, and why `core.ignorecase` exists. These files unpack **one thread at a time**.

## Pages

- [Filesystem case on major desktop OS families](filesystem/case/by/os.md) — typical Windows, macOS, and Linux behavior; no Git internals here.
