# AI example

Communication weakens when someone sends AI-generated text they did not verify.

Typical failures are wrong facts, generic tone, and claims that do not match the real context.

The fix is simple: keep ownership of intent, edit for audience, and verify key details before sending.
