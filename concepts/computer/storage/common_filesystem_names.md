# Common filesystem names (quick reference)

This **title** is kept so old links still land somewhere useful.

The content **moved** to a **hub plus one file per filesystem** so links target **stable paths** instead of heading anchors—see [Common filesystems (cross-platform)](common_filesystems.md), the [index inside `common_filesystems/`](common_filesystems/README.md), and the **glossary packet split** on [Recursive decomposition — practice checklist](../../concept/recursive_decomposition_practice.md) (with [Kitchen sink page](../../concept/kitchen_sink_page.md)).
