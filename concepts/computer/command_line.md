# The command line

**Goal:** understand what the command line is—no setup, no commands to remember yet.

Think of it as **talking to your computer by typing** instead of clicking. You type a short line, press Enter, and the computer answers in the same window. You are still using the **same computer**; this is just another way in.

That typing often happens in a window called **Terminal** (on [macOS](README.md#apple-computers-read-this-if-any-of-this-is-new), Apple’s system software). Help articles sometimes say “paste this line into Terminal”—you do not need to know how it works inside; you run the line and read the result.

Many guides use typing because **everyone follows the same words**, which is simpler than describing a long trail of clicks.

When you want to try a few safe steps on an **Apple computer running macOS**, go to [Using the terminal](using_the_terminal.md).

---

## See also

- [Spaces in file names and the command line](filename_spaces_command_line.md) — how the shell splits on spaces and why that affects paths.

---

**Check your understanding:** In your own words, what happens after you type a line and press Enter in a terminal window?
